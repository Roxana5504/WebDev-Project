import { type InlineConfig } from 'vite';
import type { ExecutionEnvironment, Target } from './types.js';
export declare class ViteServer {
    #private;
    constructor(options: InlineConfig);
    start(target: Target): Promise<ExecutionEnvironment>;
    stop(): Promise<void>;
}
